<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.novas.diseasepredict" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.novas.activity.HomeActivity"

            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.novas.activity.ResultActivity"></activity>
        <activity android:name="com.novas.activity.PageActivity"></activity>
        <activity android:name="com.novas.activity.UserinfoActivity"></activity>
        <activity android:name="com.novas.activity.userListActivity"></activity>
        <activity android:name="com.novas.activity.userTimeActivity"></activity>
        <activity android:name="com.novas.activity.LookPageListActivity"></activity>
    </application>
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>
